#include "juk3.h"

int		main()
{
	char str[] = "remain calm and keep coding!";
	char str2[] = " awl should match below v";

	ft_strrev(str);
	ft_strrev(str2);	
	ft_putstr(str);
	ft_putchar('\n');
	ft_putstr(str2);
	ft_putchar('\n');
	return (0);
}
